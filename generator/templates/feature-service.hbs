import { Injectable } from '@nestjs/common';

// Compose generated entity services here by injecting them like any other
// provider (e.g. constructor(private readonly productService: ProductService)).
// This file lives under src/features, so business logic built here never
// touches the generated CRUD modules under src/modules.
@Injectable()
export class {{pascalCase name}}Service {}
